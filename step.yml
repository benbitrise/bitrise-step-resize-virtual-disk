title: |-
  resize-virtual-disk
summary: |
  If your workflow requires more disk space, use this step to claim additional space for the virtual machine disk. This step is only comptabile with macOS stacks.
description: |
  If your workflow requires more disk space, use this step to claim additional space for the virtual machine disk. This step is only comptabile with macOS stacks.
website: https://github.com/benbitrise/bitrise-step-resize-virtual-disk
source_code_url: https://github.com/benbitrise/bitrise-step-resize-virtual-disk
support_url: https://github.com/benbitrise/bitrise-step-resize-virtual-disk/issues

project_type_tags:
  - ios
  - macos
#   - android
  - xamarin
  - react-native
  - cordova
  - ionic
  - flutter

type_tags:
  - utility

is_always_run: false
is_skippable: false
run_if: .IsCI

toolkit:
  bash:
    entry_file: step.sh

inputs:
  - blabla: "blabla"
    opts:
      title: "Bla Bla"
      description: |
        Bla bla
      is_expand: true
      is_required: false
outputs:
  - BLA: ""
    opts:
      title: "Bla"
      summary: "bla."
      description: "Bla."
      is_expand: false